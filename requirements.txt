# ============================================================================
# BandiBot Runtime Requirements
# ============================================================================
#
# Supported Python: 3.11+
#
# Install:
#   pip install -r requirements.txt
#
# System packages required outside pip:
#   - FFmpeg available on PATH
#   - espeak-ng available on PATH when TTS_PROVIDER is "kokoro"
#
# Versioning policy:
#   - Upper bounds protect the bot from unplanned major-version API changes.
#   - Lower bounds reflect modern releases needed by the current codebase.
#   - For production deployments, generate a lockfile from this manifest with
#     pip-tools, uv, Poetry, or your preferred dependency manager.
#
# ============================================================================


# ----------------------------------------------------------------------------
# Discord Gateway, Voice, And HTTP Runtime
# ----------------------------------------------------------------------------

discord.py[voice]>=2.4,<3.0
# The published package is currently pre-release-only; include the alpha
# version explicitly so clean Python 3.11/3.12 installs can resolve it.
discord-ext-voice-recv>=0.4.2a145,<1.0
aiohttp>=3.9,<4.0
PyNaCl>=1.5,<2.0


# ----------------------------------------------------------------------------
# LLM, Speech-To-Text, And Text-To-Speech Providers
# ----------------------------------------------------------------------------

openai>=1.60,<2.0
deepgram-sdk>=3.7,<4.0


# ----------------------------------------------------------------------------
# Local Wake Word Detection, VAD, And Audio ML Runtime
# ----------------------------------------------------------------------------

openwakeword>=0.6,<1.0
onnxruntime>=1.17,<2.0
silero-vad>=5.1,<7.0
torch>=2.3,<3.0
numpy>=1.26,<3.0
sentence-transformers>=3.0,<6.0


# ----------------------------------------------------------------------------
# Optional Local TTS Provider
# ----------------------------------------------------------------------------
#
# BandiBot currently defaults to Kokoro in core/config.py. Keep this installed
# unless TTS_PROVIDER is changed to "deepgram" and Kokoro imports are removed or
# guarded in deployment.

kokoro>=0.9,<1.0


# ----------------------------------------------------------------------------
# Music Resolution, Metadata, And Image Rendering
# ----------------------------------------------------------------------------

yt-dlp>=2025.1.15,<2027.0
mutagen>=1.47,<2.0
Pillow>=10.3,<12.0


# ----------------------------------------------------------------------------
# Configuration And Python Compatibility
# ----------------------------------------------------------------------------

python-dotenv>=1.0,<2.0
tzdata>=2024.1,<2027.0
audioop-lts>=0.2,<1.0; python_version >= "3.13"
